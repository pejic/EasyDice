<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EasyDice" parent="ThemeOverlay.MaterialComponents.Dark">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_actionbar_background_color</item>
        <item name="colorPrimaryDark">@color/primary_actionbar_background_color_dark</item>
        <item name="colorPrimaryVariant">@color/primary_actionbar_background_color</item>
        <item name="colorOnPrimary">@color/primary_on_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">?attr/colorPrimary</item>
        <item name="colorSecondaryVariant">?attr/colorPrimaryVariant</item>
        <item name="colorOnSecondary">?attr/colorOnPrimary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/activity_background</item>
        <item name="android:actionBarStyle">@style/AppTheme.ActionBarStyle</item>
        <item name="android:textColorLink">@color/primary_on_color</item>
    </style>

    <style name="Theme.EasyDice.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme.EasyDice.ActionBar" parent="Theme.EasyDice">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowActionBar">true</item>
    </style>

    <style name="Theme.EasyDice.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EasyDice.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>